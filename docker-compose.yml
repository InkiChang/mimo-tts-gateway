services:
  mimo-tts-gateway:
    build: .
    container_name: mimo-tts-gateway
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/data
    restart: unless-stopped
